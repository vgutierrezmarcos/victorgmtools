% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/funciones_comercio.R
\name{get_tariffs_plt}
\alias{get_tariffs_plt}
\title{Gráfico de barras de aranceles WITS/TRAINS}
\usage{
get_tariffs_plt(
  .datos_df = NULL,
  .reporter = NULL,
  .partner = "000",
  .product = "all",
  .year = "all",
  .vista = "producto",
  .tipo_arancel = NULL,
  .top_n = 15,
  .ordenar = TRUE,
  .estatico = FALSE,
  .title = NULL,
  .subtitle = NULL,
  .caption = "Fuente: WITS (Banco Mundial)"
)
}
\arguments{
\item{.datos_df}{Tibble. Dataframe con datos de aranceles (resultado de \code{\link[=get_tariffs]{get_tariffs()}}). Si se proporciona, se ignoran \code{.reporter}, \code{.partner}, \code{.product} y \code{.year}. Por defecto, \code{NULL}.}

\item{.reporter}{Cadena de caracteres. Código ISO3 del país declarante. Se usa cuando \code{.datos_df} es \code{NULL}. Por defecto, \code{NULL}.}

\item{.partner}{Cadena de caracteres. Código ISO3 del país socio, o \code{"000"} (mundo). Por defecto, \code{"000"}.}

\item{.product}{Cadena de caracteres. Código de producto HS6 o \code{"all"}. Por defecto, \code{"all"}.}

\item{.year}{Cadena de caracteres o numérico. Año(s) de consulta. Por defecto, \code{"all"}.}

\item{.vista}{Cadena de caracteres. Tipo de agrupación: \code{"producto"} (capítulos HS2), \code{"socio"} (países socios), \code{"anual"} (por año). Por defecto, \code{"producto"}.}

\item{.tipo_arancel}{Cadena de caracteres. Filtrar por tipo de arancel (p.ej. \code{"MFN"}, \code{"Pref"}). Si \code{NULL}, se muestran todos. Por defecto, \code{NULL}.}

\item{.top_n}{Número entero. Número máximo de barras a mostrar. Por defecto, \code{15}.}

\item{.ordenar}{Lógico. Si \code{TRUE}, ordena las barras de mayor a menor. Por defecto, \code{TRUE}.}

\item{.estatico}{Lógico. Si \code{TRUE}, devuelve un gráfico estático. Si \code{FALSE}, interactivo (\code{ggiraph}). Por defecto, \code{FALSE}.}

\item{.title}{Cadena de caracteres. Título del gráfico. Por defecto, \code{NULL} (se genera automáticamente).}

\item{.subtitle}{Cadena de caracteres. Subtítulo. Por defecto, \code{NULL}.}

\item{.caption}{Cadena de caracteres. Nota al pie. Por defecto, \code{"Fuente: WITS (Banco Mundial)"}.}
}
\value{
Un objeto \code{ggplot} (estático) o \code{girafe} (interactivo).
}
\description{
Genera un gráfico de barras (horizontal) con datos de aranceles. Acepta un dataframe
previamente obtenido con \code{\link[=get_tariffs]{get_tariffs()}} o descarga los datos al vuelo.
}
\examples{
\dontrun{
get_tariffs_plt(.reporter = "ESP", .year = "2022", .vista = "producto")
datos <- get_tariffs(reporter = "USA", partner = "000", year = 2022)
get_tariffs_plt(.datos_df = datos, .vista = "producto", .top_n = 10)
}
}
